version: "3.8"

services:
  # --- Database Service ---
  db:
    image: mysql:8.0
    container_name: university_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${DB_PORT}:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - university_net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --- Object Storage Service ---
  minio:
    image: minio/minio:latest
    container_name: university_minio
    restart: always
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "${MINIO_API_PORT}:9000"     # API Port
      - "${MINIO_CONSOLE_PORT}:9001" # Console Port
    volumes:
      - minio_data:/data
    networks:
      - university_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  # --- Backend Service (Plan for Django) ---
  # Currently commented out until Dockerfile is created
  # backend:
  #   build: ./backend
  #   container_name: university_backend
  #   command: python manage.py runserver 0.0.0.0:8000
  #   volumes:
  #     - ./backend:/app
  #   ports:
  #     - "${BACKEND_PORT}:8000"
  #   env_file:
  #     - .env
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #     minio:
  #       condition: service_healthy
  #   networks:
  #     - university_net

networks:
  university_net:
    driver: bridge

volumes:
  db_data:
  minio_data:
